#By Voli
[Unit]
Description=Docker-compose: %i, by Voli

After   =voli-docker-stop.service
Requires=voli-docker-stop.service


[Service]
Type=oneshot
RemainAfterExit=yes
SyslogIdentifier=%N

WorkingDirectory=/etc/voli-env/docker

ExecStart=/usr/bin/docker-compose --no-ansi -f %i.yaml up -d
ExecStop =/usr/bin/docker-compose --no-ansi -f %i.yaml down


[Install]
WantedBy=multi-user.target
